{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Plugin Jar",
            "type": "shell",
            "command": "./gradlew",
            "args": [
                "--console=plain",
                "shadowJar"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": "build",
            "problemMatcher": []
        }
    ]
}
